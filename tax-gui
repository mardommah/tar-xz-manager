#!/usr/bin/env python3
"""Tax Installer — GUI entry point."""

import sys
from pathlib import Path

# Ensure we can import from the same directory
sys.path.insert(0, str(Path(__file__).resolve().parent))

from view import main

main()
